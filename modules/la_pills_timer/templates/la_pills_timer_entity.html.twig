{#
/**
 * @file la_pills_timer_entity.html.twig
 * Default theme implementation to present LA Pills Timer data.
 *
 * This template is used when viewing LA Pills Timer pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_la_pills_timer_entity()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('la_pills_timer_entity') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
