{#
/**
 * @file la_pills_user_package.html.twig
 * Default theme implementation to present User package data.
 *
 * This template is used when viewing User package pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_la_pills_user_package()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('la_pills_user_package') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
